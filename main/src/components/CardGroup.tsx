function CardGroup() {
  // use map to group cards tgther, maybe can apply the same to App.tsx to map routes
  // to do: make a way to compress images from google drive quick to png and host locally..
  // make it able to expand and for users to click to open original file link
  return <div>CardGroup</div>;
}

export default CardGroup;
